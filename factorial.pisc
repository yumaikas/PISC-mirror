/* TODO: Fix what's broken this thing... */
: factorial ( x -- y ) :x 
	$x 0 >= [ $x $x 1 - factorial * ] [ 1 ] if ;

: factorial-loop ( x -- y ) dup :x :result 
 $x 1 - [
    $x 1 - :x
	$result $x * :result
 ] times 
 $result
 ;: test ( x -- y ) dup print 1 - :x $x 0 > [ $x test ] [ $x ] if ;